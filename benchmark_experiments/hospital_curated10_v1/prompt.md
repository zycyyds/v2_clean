请执行基于标准示例的单表数据纠错任务。

你只能根据 train raw 与 train reference 中的10个成对示例，自主发现脏数据与标准数据之间的差异规律，并生成一个可重放的清洗 pipeline。当前公开输入只有一张表；不要假设必须使用跨表规则。

要求：
1. 不要预设、猜测或列举当前数据集的具体错误清单。
2. 自主比较成对示例，并结合完整公开脏表中的重复值、字段依赖和一致性规律推导修复方法。
3. 只在有充分证据时修改数据，无法唯一推出正确值时保持原值。
4. 保持原始文件名、列名、列顺序、行数、行顺序和CSV格式。
5. 必须输出完整数据表，不能只输出被修改的行。
6. 不得搜索历史实验、隐藏标准答案、私有差异清单、评分结果或网络资源。
7. 四类数据质量 skills 仅作为非穷尽分析视角，必须用当前成对示例和公开脏表验证后才能应用。
8. 将发现和确定性修复逻辑保存在当前 workspace，最终写入 submission.json 并发布完整 result_package。
